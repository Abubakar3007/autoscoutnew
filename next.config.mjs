/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ["images10.gaadi.com", "stimg.cardekho.com"]
  }
};

export default nextConfig;
